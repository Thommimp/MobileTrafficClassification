# TDC NET Mobile Traffic Classification (MTC)

To get started make sure you have the following

- `uv` <https://docs.astral.sh/uv/getting-started/installation/>
- `git-lfs`

## Getting started

Install all dependencies with

```shell
uv sync
```

And pull data with

```shell
git lfs pull
```
